---
phase: 02.1-fork-shell
plan: 03
type: execute
wave: 3
depends_on: ["02.1-01", "02.1-02"]
files_modified:
  - modules/nixos/stylix.nix
  - modules/nixos/hyprland.nix
  - hosts/nervos/default.nix
  - modules/home/hyprland.nix
autonomous: false

must_haves:
  truths:
    - "`nixos-rebuild switch --flake .#nervos` succeeds on the NixOS target"
    - "caelestia-shell systemd user service is active after rebuild"
    - "caelestia launcher / clipboard / screenshot keybinds work on the live system"
    - "No duplicate daemons (no waybar/mako/hyprpaper alongside caelestia)"
    - "Lock screen invocation (Super+L) shows a working lock UI"
    - "stylix.targets.hyprlock is disabled if caelestia does not reuse hyprlock; kept enabled if it does"
    - "grimblast, cliphist, and hyprpaper system packages are removed if caelestia-cli fully replaces them"
  artifacts:
    - path: "hosts/nervos/default.nix"
      provides: "system package list updated (grimblast/cliphist/hyprpaper removed if superseded)"
    - path: "modules/nixos/stylix.nix"
      provides: "stylix.targets.hyprlock explicitly set based on checkpoint finding"
    - path: ".planning/phases/02.1-fork-shell/02.1-03-SUMMARY.md"
      provides: "Post-rebuild decisions: caelestia CLI subcommand grammar, hyprlock reuse Y/N, packages removed"
  key_links:
    - from: "systemctl --user status caelestia-shell"
      to: "caelestia QuickShell process"
      via: "graphical-session.target"
      pattern: "active \\(running\\)"
    - from: "Super+L (loginctl lock-session)"
      to: "active lock surface (caelestia or hyprlock)"
      via: "systemd-logind lock contract"
---

<objective>
Rebuild the NixOS system with the new caelestia shell, verify the live environment, and resolve the four open questions the Research document flagged (caelestia CLI grammar, hyprlock reuse, stylix.targets.hyprlock fate, removable system packages). This plan intentionally pauses at a human-verify checkpoint — rebuild outcomes cannot be evaluated from the planner side.

Purpose: Plans 01 and 02 ship code that evaluates cleanly (nix flake check passes) but has never been run. This plan is the integration gate: rebuild, look at the screen, run `caelestia --help`, decide, then clean up the last pieces of the old stack.
Output: Live caelestia shell on NixOS, an updated stylix.nix reflecting hyprlock-target decision, a cleaned hosts/nervos/default.nix system package list, and a SUMMARY documenting the resolved open questions.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02.1-fork-shell/02.1-RESEARCH.md
@.planning/phases/02.1-fork-shell/02.1-01-SUMMARY.md
@.planning/phases/02.1-fork-shell/02.1-02-SUMMARY.md
@modules/nixos/stylix.nix
@modules/nixos/hyprland.nix
@hosts/nervos/default.nix
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build the flake and stage it for rebuild</name>
  <files>(no file writes; dry-run build only)</files>
  <action>
Do NOT run `nixos-rebuild switch` from the planner host — the repo lives on Windows and the target is a NixOS VM. Instead, validate that the flake builds to a complete system closure.

1. Run `nix flake check --no-build` — must exit 0. If it fails, read the error and fix in the appropriate plan (do NOT patch here; open a gap closure issue).

2. Run `nix build .#nixosConfigurations.nervos.config.system.build.toplevel --no-link --print-out-paths` (or the equivalent dry-run if running on Windows cannot execute this — in that case, log "dry-run not possible on Windows host; deferred to checkpoint in Task 2" and proceed).

3. If the build succeeds: record the store path in the SUMMARY. If the build fails with a caelestia eval error: record the error verbatim and STOP — this task fails and the checkpoint in Task 2 cannot proceed until flake builds. Do NOT try to patch caelestia's module; the contract is "trust upstream, configure via options."

Acceptable outcomes: (a) build succeeds and prints a store path, or (b) build cannot run on this host — caller will run it on the NixOS target at the checkpoint.
  </action>
  <verify>
`nix flake check --no-build` exits 0. Either `nix build ...toplevel --no-link` produces a store path, or the environment legitimately cannot run it (Windows host without nix).
  </verify>
  <done>
Flake evaluates to a complete NixOS system closure OR it's documented that the build step must happen on the NixOS target. No code changes in this task — purely a gate.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Rebuild on NixOS target, verify live shell, run caelestia --help</name>
  <what-built>
Phase 2.1 Plans 01 + 02 committed:
- caelestia-shell flake input pinned in flake.lock
- modules/home/caelestia.nix enables programs.caelestia (systemd + cli)
- modules/home/default.nix imports caelestia; waybar/rofi/mako/lock/wallpaper/clipboard deleted
- modules/home/hyprland.nix keybinds reroute SPACE/V/Print/SHIFT+Print through caelestia
  </what-built>
  <how-to-verify>
Run these on the NixOS target (VM) in order. Record output for each in the resume signal.

1. **Rebuild:**
   ```
   sudo nixos-rebuild switch --flake .#nervos
   ```
   Expected: completes without error. If it fails, paste the full error in the resume signal; this plan cannot proceed.

2. **Verify caelestia service is running:**
   ```
   systemctl --user status caelestia-shell
   ```
   Expected: `active (running)`, bound to graphical-session.target. If it's failed, run `journalctl --user -u caelestia-shell -b` and capture the last 30 lines.

3. **Visual check — bar:**
   - Log out and log back in (or reboot) to let graphical-session.target re-fire cleanly.
   - Expect: exactly ONE bar on screen (no waybar+caelestia overlap). If two bars appear, Plan 01 deletions didn't take — check `ls modules/home/` and confirm waybar.nix is gone AND `git status` is clean.

4. **Keybind checks:**
   - Super+SPACE → launcher drawer opens. If nothing happens, note it; the subcommand string is a Plan 02 guess.
   - Super+V → clipboard UI opens.
   - PrtSc → screenshot region selector.
   - Shift+PrtSc → full-screen screenshot.
   - Super+L → lock screen appears. Unlock with pilot password.
   - Super+T → kitty still opens.
   - Super+1..9 → workspace switching still works.

5. **Run `caelestia --help` and `caelestia shell --help`** — paste the full subcommand tree output in the resume signal. This is the authoritative source for question (2) in the Research document.

6. **Check which binary handles lock:**
   ```
   systemctl --user list-units --all | grep -iE 'lock|caelestia|hyprlock'
   ps aux | grep -iE 'lock|caelestia' | grep -v grep
   loginctl lock-session   # trigger it, then inspect
   ```
   Record: does caelestia spawn hyprlock underneath, or does it run its own lock surface? This answers Open Question 1.

7. **Check stylix.targets.hyprlock state:**
   ```
   ls ~/.config/hypr/hyprlock.conf 2>/dev/null && echo "stylix wrote hyprlock config" || echo "no hyprlock config"
   nix eval .#nixosConfigurations.nervos.config.stylix.targets.hyprlock.enable
   ```
   Record both outputs.

**Resume signal format:**
```
rebuild: [ok | FAILED: <error>]
service: [active | FAILED: <logs>]
bar_count: [1 | 2 | 0]
keybinds:
  super_space: [works | broken: <observation>]
  super_v:     [works | broken: ...]
  prtsc:       [works | broken: ...]
  shift_prtsc: [works | broken: ...]
  super_l:     [locks + unlocks | broken: ...]
caelestia_help: |
  <paste full output>
caelestia_shell_help: |
  <paste full output>
lock_backend: [hyprlock | caelestia-internal | other: ...]
stylix_hyprlock_target: [enabled + writes config | enabled + no config | disabled]
packages_to_remove: [grimblast-ok, cliphist-ok, hyprpaper-not-present, etc.]
issues: [none | list unexpected observations]
```

Paste every section. Task 3 uses this to compute the final code changes; a partial checkpoint means Task 3 can't finalize.
  </how-to-verify>
  <resume-signal>Paste the full checklist output above, or describe blocking issues.</resume-signal>
  <files>(no file writes — live-system verification only)</files>
  <action>Execute the rebuild + verification checklist in <how-to-verify> above on the NixOS target. This task produces no file changes; its output is the paste-back that Task 3 consumes.</action>
  <verify>All seven checklist items executed and output pasted into the resume signal.</verify>
  <done>Resume signal received with every field populated; Task 3 has enough information to make the stylix/package/keybind decisions.</done>
</task>

<task type="auto">
  <name>Task 3: Resolve open questions — update stylix target, system packages, keybinds</name>
  <files>modules/nixos/stylix.nix, hosts/nervos/default.nix, modules/home/hyprland.nix (if keybinds need correction)</files>
  <action>
Read the checkpoint output from Task 2. Make the following edits based on what was observed:

**A. stylix.targets.hyprlock (decision driven by `lock_backend` field):**

- If `lock_backend: hyprlock` (caelestia reuses hyprlock underneath): leave `modules/nixos/stylix.nix` alone. Stylix still themes hyprlock, which is still active.
- If `lock_backend: caelestia-internal` (caelestia runs its own QML lock, hyprlock is unused): add to `modules/nixos/stylix.nix` at the top level of the `stylix` attrset:
  ```nix
  targets.hyprlock.enable = false;
  ```
  And in `modules/nixos/hyprland.nix`, change `programs.hyprlock.enable = true;` to `programs.hyprlock.enable = false;` (or delete the line entirely if no other config references it). Keep hypridle out of scope — it was in the deleted modules/home/lock.nix and is already gone.
- If `lock_backend: other` (unexpected): STOP, record in SUMMARY, do NOT guess.

**B. System package cleanup in hosts/nervos/default.nix:**

Based on `packages_to_remove` field, remove from `environment.systemPackages`:
- `grimblast` + `grim` + `slurp` + `hyprpicker` — IF `caelestia screenshot region/screen` works in the checkpoint.
- `jq` + `libnotify` — KEEP (general utilities, not only grimblast deps).
- `wl-clipboard` — KEEP (caelestia-cli uses it internally; system-wide utility).
- `brightnessctl` + `playerctl` — KEEP (hyprland bindel/bindl still call them).

For each package removed, leave a one-line comment explaining why (e.g., `# grimblast removed: caelestia-cli screenshot supersedes`).

If the screenshot keybinds FAILED in the checkpoint (caelestia subcommand grammar differs), do NOT remove grimblast yet — instead, update `modules/home/hyprland.nix` screenshot binds to the correct grammar from `caelestia shell --help`, re-enter the rebuild loop mentally (note in SUMMARY that a re-rebuild is needed), and only then remove the packages.

**C. Launcher/clipboard keybind correction (if needed):**

If the `caelestia_help` output shows that the subcommand strings in Plan 02 are wrong (e.g., `caelestia shell drawers toggle launcher` isn't a real command), edit `modules/home/hyprland.nix` to use the correct grammar. Do this FIRST, before package cleanup, so that the final rebuild reflects working keybinds.

**D. Record resolutions in SUMMARY:**

All four Research open questions must be answered in the Plan 03 SUMMARY:
1. Does caelestia's lock reuse hyprlock? → resolved by `lock_backend` field
2. Exact CLI subcommand grammar → captured from `caelestia_help` and `caelestia_shell_help`
3. `programs.caelestia.systemd.enable` default → inferred from whether Plan 01's explicit `systemd.enable = true` was needed or redundant
4. stylix.targets.hyprlock interaction → resolved by Step A

Run `nix flake check --no-build` at the end. If this task modifies files, the user must re-run `sudo nixos-rebuild switch` to apply — note this in the SUMMARY as a follow-up action, but do not checkpoint again (diminishing returns; if something obvious is broken user will re-run /gsd:plan-phase 02.1 --gaps).
  </action>
  <verify>
`nix flake check --no-build` exits 0 after edits.
If stylix.targets.hyprlock was changed: `grep 'targets.hyprlock' modules/nixos/stylix.nix` shows the new value.
If packages were removed: `grep -c 'grimblast' hosts/nervos/default.nix` returns 0 (or 1 if kept with a comment).
SUMMARY file addresses all four Research open questions.
  </verify>
  <done>
All four Research open questions are resolved and documented. Dead code (unused stylix targets, superseded system packages, or wrong keybind grammar) is cleaned up based on live observations. Flake still evaluates cleanly. User knows whether one final nixos-rebuild switch is still needed.
  </done>
</task>

</tasks>

<verification>
- `nix flake check --no-build` passes.
- No waybar/rofi/mako/cliphist/grimblast references survive in any modules/* file (spot-check via `grep -rE 'waybar|rofi|mako|cliphist|grimblast' modules/ hosts/` — only allowed matches are removal comments).
- SUMMARY file answers all four Research open questions with concrete evidence from the checkpoint output.
- If stylix.targets.hyprlock = false was set: `nix eval .#...stylix.targets.hyprlock.enable` returns `false`.
</verification>

<success_criteria>
1. Live caelestia shell runs on the NixOS target (verified by `systemctl --user status`).
2. Primary keybinds (launcher, clipboard, screenshot, lock, terminal) all work.
3. stylix.targets.hyprlock decision is made and reflected in modules/nixos/stylix.nix.
4. Superseded system packages (grimblast + deps) are removed from hosts/nervos/default.nix once caelestia-cli equivalents are confirmed working.
5. The Plan 03 SUMMARY documents resolutions to Research Open Questions 1-4 with live evidence.
6. No "Phase 2.1 half-done" code paths remain — either a thing is caelestia-native or it's explicitly kept with a comment saying why.
</success_criteria>

<output>
After completion, create `.planning/phases/02.1-fork-shell/02.1-03-SUMMARY.md`. Include:
- Exact `caelestia --help` and `caelestia shell --help` output (this becomes the canonical reference for all future phases that need caelestia subcommand strings)
- Lock backend finding (hyprlock vs caelestia-internal) with supporting evidence from systemctl/ps
- Final state of stylix.targets.hyprlock (enabled/disabled, with justification)
- List of removed system packages with per-package reason
- Any keybind corrections applied
- Whether a follow-up `nixos-rebuild switch` is required before Phase 3 resumes
</output>
