---
phase: 02.1-fork-shell
plan: 01
subsystem: infra
tags: [nix, flake, home-manager, caelestia, quickshell, hyprland]

requires:
  - phase: 01-flake-foundation
    provides: flake.nix outputs with home-manager.nixosModules + stylix wired
  - phase: 02-desktop-shell
    provides: modules/home/{waybar,rofi,mako,lock,wallpaper,clipboard}.nix (now superseded)
provides:
  - caelestia-shell pinned flake input following nixpkgs
  - home-manager.sharedModules wiring for caelestia.homeManagerModules.default
  - modules/home/caelestia.nix with programs.caelestia (enable + systemd + cli + minimal settings)
  - cleaned modules/home/default.nix (hyprland + kitty + caelestia only)
  - removal of six hand-rolled home modules (waybar/rofi/mako/lock/wallpaper/clipboard)
affects: [02.1-02, 02.1-03, 03, 05, 07]

tech-stack:
  added: [caelestia-shell (github:caelestia-dots/shell), QuickShell (transitive)]
  patterns:
    - "One flake input + sharedModules line replaces N per-component daemons"
    - "Atomic delete+add to prevent mid-state with both old and new shells"
    - "Do NOT add pkgs.quickshell manually (Research Pitfall 1 - version skew)"

key-files:
  created: [modules/home/caelestia.nix, .planning/phases/02.1-fork-shell/02.1-01-SUMMARY.md]
  modified: [flake.nix, modules/home/default.nix]
  deleted: [modules/home/waybar.nix, modules/home/rofi.nix, modules/home/mako.nix, modules/home/lock.nix, modules/home/wallpaper.nix, modules/home/clipboard.nix]

key-decisions:
  - "caelestia-shell follows nixpkgs only (flake does not consume home-manager)"
  - "systemd.enable = true + target = graphical-session.target (mirrors Phase 2 waybar pattern); no Hyprland exec-once (would double-start)"
  - "cli.enable = true so Hyprland keybinds in Plan 02 can invoke `caelestia` CLI"
  - "Behaviour-only settings in caelestia.nix; fonts/colors deferred -- caelestia themes its own QML (Stylix still owns kitty/GTK/Qt)"
  - "NixOS-side programs.hyprlock.enable stays (PAM); only home-manager hyprlock config removed. NixOS-side resolution deferred to Plan 03."
  - "Delete six modules via `git rm` atomically with caelestia.nix creation -- never an evaluable mid-state with both shells defined"

patterns-established:
  - "Fork-shell pattern: single upstream flake + one home-manager module collapses many daemons"
  - "sharedModules path for plugging an upstream home-manager module without importing it inside ./modules/home"

duration: 2min
completed: 2026-04-17
---

# Phase 02.1 Plan 01: Wire caelestia-shell into NervOS flake Summary

**Collapsed waybar + rofi + mako + hyprlock config + hyprpaper + cliphist (six home-manager modules) into a single caelestia-shell flake input + one home-manager module, via home-manager.sharedModules.**

## Performance

- **Duration:** ~2 min
- **Started:** 2026-04-17T18:22:54Z
- **Completed:** 2026-04-17T18:24:09Z
- **Tasks:** 2 / 2
- **Files modified:** 2 (flake.nix, modules/home/default.nix)
- **Files created:** 1 (modules/home/caelestia.nix)
- **Files deleted:** 6 (waybar/rofi/mako/lock/wallpaper/clipboard .nix)

## Accomplishments
- caelestia-shell pinned as a flake input following nixpkgs, destructured in outputs
- home-manager.sharedModules wired to caelestia.homeManagerModules.default
- modules/home/caelestia.nix enables programs.caelestia with systemd user service on graphical-session.target, CLI on PATH, and minimal behaviour-only settings (bar battery + wallpaperDir)
- modules/home/default.nix imports collapsed to hyprland + kitty + caelestia (4 files total in modules/home)
- Six superseded home modules removed via `git rm` (tracked deletion, not orphan files)

## Task Commits

1. **Task 1: Add caelestia-shell flake input and wire homeManagerModules.default** - `259a3d2` (feat)
2. **Task 2: Create caelestia.nix module, update default.nix imports, delete superseded modules** - `d0fc1f4` (feat)

## Files Created/Modified
- `flake.nix` - Added caelestia-shell input (follows nixpkgs), destructured in outputs, wired sharedModules
- `modules/home/caelestia.nix` - New: programs.caelestia.enable + systemd + cli + minimal settings
- `modules/home/default.nix` - Imports list collapsed to hyprland/kitty/caelestia
- `modules/home/waybar.nix` - DELETED (superseded by caelestia bar)
- `modules/home/rofi.nix` - DELETED (superseded by caelestia launcher)
- `modules/home/mako.nix` - DELETED (superseded by caelestia notifications)
- `modules/home/lock.nix` - DELETED (superseded by caelestia lock; NixOS programs.hyprlock stays)
- `modules/home/wallpaper.nix` - DELETED (superseded by caelestia-cli wallpaper watcher)
- `modules/home/clipboard.nix` - DELETED (superseded by `caelestia clipboard`)

## Decisions Made
- Followed plan's sharedModules wiring exactly rather than per-host imports (cleaner for eventual specialisations).
- caelestia input follows nixpkgs only (plan called this out; confirmed not to follow home-manager).
- Did NOT add `pkgs.quickshell` to environment.systemPackages (Research Pitfall 1 explicitly prohibits this).

## Deviations from Plan

None structurally. Plan executed exactly as written, with one environmental deferral (see below) that is acknowledged by the plan's own text ("If the command fails due to network/sandbox, run `nix flake update` instead").

## Issues Encountered

**Nix toolchain unavailable on Windows dev host** — `nix flake lock` / `nix flake check --no-build` cannot be executed on this environment (the development checkout is Windows; Nix only exists on the NixOS target and on `nix`-bearing Linux hosts).

Mitigations:
- All static/grep verifications in the plan were executed and pass: `grep -q 'caelestia-shell' flake.nix`, `grep -q 'sharedModules' flake.nix`, `grep -c './caelestia.nix' modules/home/default.nix` (= 1), `modules/home/` contents = {caelestia.nix, default.nix, hyprland.nix, kitty.nix}, `git status` shows exactly the expected create/modify/delete set.
- flake.lock is absent in the working tree (no prior lockfile committed). It will be generated on first `nix` invocation on the NixOS target; the caelestia-shell entry will appear at that point.
- Full `nix flake check --no-build` verification is deferred to the NixOS target rebuild (natural next step before Plan 02 begins). This is documented in both task commit messages.

## User Setup Required

On the NixOS target before Plan 02 execution:
1. `nix flake lock` (generates flake.lock with pinned caelestia-shell rev)
2. `nix flake check --no-build` (must exit 0)
3. `sudo nixos-rebuild switch --flake .#nervos`

If `nix flake check` surfaces a caelestia module schema error (e.g., settings path depth differs from what Research drafted), fix minimally per the error message and re-run.

## Next Phase Readiness
- Plan 02 (wire Hyprland keybinds to `caelestia` CLI) can begin immediately — cli.enable=true is already set.
- Plan 03 (resolve NixOS-side hyprlock / grimblast / cliphist overlap) can begin in parallel.
- Caveat: the deferred `nix flake check` must run green on the NixOS target before merging Plan 02 work; if it fails, fixes land in this plan's context, not Plan 02's.

## Self-Check

Verifying all claims before proceeding.

- `modules/home/caelestia.nix` — FOUND
- `modules/home/default.nix` — FOUND (modified)
- `flake.nix` — FOUND (modified)
- `modules/home/waybar.nix` — CORRECTLY ABSENT
- `modules/home/rofi.nix` — CORRECTLY ABSENT
- `modules/home/mako.nix` — CORRECTLY ABSENT
- `modules/home/lock.nix` — CORRECTLY ABSENT
- `modules/home/wallpaper.nix` — CORRECTLY ABSENT
- `modules/home/clipboard.nix` — CORRECTLY ABSENT
- Commit `259a3d2` — FOUND
- Commit `d0fc1f4` — FOUND

## Self-Check: PASSED

---
*Phase: 02.1-fork-shell*
*Completed: 2026-04-17*
