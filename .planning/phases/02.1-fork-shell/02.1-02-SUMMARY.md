---
phase: 02.1-fork-shell
plan: 02
subsystem: ui
tags: [hyprland, caelestia, keybinds, shell, wayland]

# Dependency graph
requires:
  - phase: 02.1-fork-shell
    provides: caelestia CLI installed via programs.caelestia.cli.enable (Plan 01)
  - phase: 02-desktop-shell
    provides: existing Hyprland bind list (workspaces, focus, media, brightness, togglefloating)
provides:
  - Super+SPACE routed to caelestia shell drawers toggle launcher
  - Super+V routed to caelestia clipboard
  - PrtSc / Shift+PrtSc routed to caelestia screenshot region / screen
  - Super+L retained on loginctl lock-session (caelestia lock honors session-lock contract)
affects: [02.1-03-fork-shell]

# Tech tracking
tech-stack:
  added: []
  patterns:
    - "Caelestia subcommand grammar embedded in Hyprland binds (provisional; Plan 03 verifies via caelestia --help)"

key-files:
  created: []
  modified:
    - modules/home/hyprland.nix

key-decisions:
  - "Super+L kept on loginctl lock-session (same session-lock contract caelestia uses); Plan 03 can swap to direct caelestia invocation if --help reveals different grammar"
  - "Provisional caelestia subcommands: `shell drawers toggle launcher`, `clipboard`, `screenshot region`, `screenshot screen` -- flagged as LOW confidence in RESEARCH; Plan 03 decision checkpoint confirms or corrects"
  - "Surgical edit: only the five Phase-2-Plan-04 binds touched; bindel/bindl/bindm/exec-once and all workspace/focus/terminal/togglefloating binds byte-identical"

patterns-established:
  - "Pattern: provisional CLI grammar in Nix configs is marked with an inline comment pointing at the verification checkpoint so a future fix is cheap"

# Metrics
duration: 1min
completed: 2026-04-18
---

# Phase 02.1 Plan 02: Hyprland Keybind Rewire Summary

**Five Phase-2 keybinds (SPACE, V, PrtSc, SHIFT+PrtSc, L) rerouted to caelestia CLI subcommands with loginctl lock-session retained; no other bind touched.**

## Performance

- **Duration:** ~1 min
- **Started:** 2026-04-18T14:11:16Z
- **Completed:** 2026-04-18T14:11:44Z
- **Tasks:** 1
- **Files modified:** 1

## Accomplishments

- Super+SPACE now opens the caelestia launcher drawer (was `rofi -show drun`)
- Super+V now opens caelestia clipboard (was the `cliphist list | rofi -dmenu | cliphist decode | wl-copy` pipeline)
- PrtSc and Shift+PrtSc now invoke caelestia screenshot region/screen (was `grimblast copy area/screen`)
- Super+L retained on `loginctl lock-session` so caelestia lock plugs into the standard systemd session-lock contract (same interface as hyprlock)
- Every other bind -- workspace 1..9, movetoworkspace 1..9, movefocus, $terminal, killactive, exit, togglefloating (Super+SHIFT+V from Phase 2 Plan 04), fullscreen, media keys (bindel/bindl), mouse (bindm), and exec-once (nm-applet, hyprpolkitagent) -- is byte-identical

## Task Commits

1. **Task 1: Reroute Hyprland keybinds through caelestia CLI** - `5e0bbaf` (feat)

## Files Created/Modified

- `modules/home/hyprland.nix` - Replaced five rofi/cliphist/grimblast exec lines in the `bind` list with four caelestia exec lines plus the unchanged `loginctl lock-session` line; added a 3-line comment pointing at Plan 03 as the verification checkpoint.

## Exact Keybind Strings Committed

(Recorded here so Plan 03's `caelestia --help` checkpoint has a clean diff target.)

```nix
# Phase 2.1 -- caelestia shell wiring (supersedes rofi/cliphist/grimblast keybinds)
# NOTE: Exact subcommand grammar verified at Plan 03 checkpoint via `caelestia --help`.
# If any of these fail post-rebuild, Plan 03's decision checkpoint updates them.
"$mainMod, SPACE, exec, caelestia shell drawers toggle launcher"
"$mainMod, L, exec, loginctl lock-session"
"$mainMod, V, exec, caelestia clipboard"
", Print, exec, caelestia screenshot region"
"SHIFT, Print, exec, caelestia screenshot screen"
```

## Decisions Made

- Subcommand grammar matches the best-documented guesses from the caelestia cli repo subcommand list; flagged LOW confidence in RESEARCH (Open Question 2). Plan 03 reconciles.
- Super+L untouched because `loginctl lock-session` is the vendor-neutral session-lock trigger and the one caelestia's shell listens on. Swapping to `caelestia shell lock` (or similar) is a Plan 03 decision if `--help` disagrees.

## Deviations from Plan

None - plan executed exactly as written.

## Issues Encountered

None.

## Verification Results

- `grep -c 'exec, caelestia' modules/home/hyprland.nix` = 4 (launcher, clipboard, screenshot region, screenshot screen) - PASS
- `grep -c 'rofi|cliphist|grimblast' modules/home/hyprland.nix` = 0 - PASS
- `grep -c 'loginctl lock-session' modules/home/hyprland.nix` = 1 - PASS
- `grep -c 'workspace, [1-9]' modules/home/hyprland.nix` = 18 (9 workspace + 9 movetoworkspace; both trigger the regex, all binds intact) - PASS
- `grep -c 'togglefloating' modules/home/hyprland.nix` = 1 - PASS
- `nix flake check --no-build` - **DEFERRED** to NixOS target (Nix unavailable on Windows dev host; same deferral as Plan 01, tracked in STATE.md "Deferred verification")

## User Setup Required

None - no external service configuration required.

## Next Phase Readiness

- Plan 03 (validation + subcommand reconciliation) is unblocked. It runs `caelestia --help` against the live system, diffs against the five exact strings recorded above, and either confirms or corrects.
- Phase 2.1 success criteria now require only Plan 03's live-system validation to land.

## Self-Check: PASSED

Verified:
- FOUND: modules/home/hyprland.nix (modified)
- FOUND: commit 5e0bbaf (`git log --oneline -1` shows `5e0bbaf feat(02.1-02): reroute Hyprland keybinds through caelestia CLI`)
- FOUND: .planning/phases/02.1-fork-shell/02.1-02-SUMMARY.md (this file)

---
*Phase: 02.1-fork-shell*
*Completed: 2026-04-18*
